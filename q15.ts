let guestName = ["Noor", "Ayesha", "Areeba"];
let canNotAttend = "Areeba";
console.log("".concat(canNotAttend, " can not make it,for dinner"));
let newGuest = "amna";
guestName[guestName.indexOf(canNotAttend)] = newGuest;
console.log(guestName);
guestName.map(function (item) { return console.log("Hellow ".concat(item, " you are invited to dinner"));
let middleGuest = "aqsa";
let middleindex = guestName.length / 2;
guestName.splice(middleindex, 0, middleGuest);
guestName.push("hira");
console.log("we can invit only two people for dinner");
guestName.pop();
console.log(guestName);
while. (guestName.length > 2)
    ;
console.log(guestName);
